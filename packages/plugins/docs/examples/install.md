## 安装

::: code-group

```bash [npm]
npm install @wgl-m/plugins -S
```

```bash [cnpm]
cnpm install @wgl-m/plugins -S
```

```bash [pnpm]
pnpm add @wgl-m/plugins
```

```bash [yarn]
yarn add @wgl-m/plugins
```

:::

## 注意

`@wgl-m/plugins` **仅适用于 Browser 环境**，请勿在 Node.js 中引用。

`upload` 插件可选依赖 `axios`，按需安装：

```bash
pnpm add axios
```
