# 08 - 确认清单

> 请逐项确认，回复你的选择后我再开始执行。

---

## 一、范围确认

- [ ] **A1.** 迁移 `packages/utils` 源码为 TypeScript
- [ ] **A2.** 迁移 `packages/plugins` 源码为 TypeScript
- [ ] **A3.** 迁移 `packages/node-utils` 源码为 TypeScript
- [ ] **A4.** `packages/css` 不迁移（保持 Gulp + SCSS）
- [ ] **A5.** VitePress 文档站（`packages/*/docs`）不迁移

> 默认：A1～A4 全选 ✅，A5 不迁移 ✅

---

## 二、构建工具

- [ ] **B1.** utils 从 Webpack 5 迁移到 tsup（推荐）
- [ ] **B2.** utils 保留 Webpack，仅加 TS loader（不推荐，维护两套）

> 默认：B1 ✅

---

## 三、ES5 UMD 产物

utils 当前输出 `dist/main.js`（ES5 全局变量 `Wgl`）。

- [ ] **C1.** 移除 ES5 产物，发 `@wgl-m/utils@3.0.0` major（推荐）
- [ ] **C2.** 保留 ES5 产物，发 minor 版本

> 默认：C1 ✅

---

## 四、TypeScript 严格模式

- [ ] **D1.** 初期 `strict: false`，迁移完成后再开（推荐）
- [ ] **D2.** 直接 `strict: true`

> 默认：D1 ✅

---

## 五、执行阶段

- [ ] **E1.** 全部 Phase 1～5 一次性执行
- [ ] **E2.** 分阶段执行，每阶段确认后再继续
- [ ] **E3.** 仅 Phase 1～2（基础设施 + node-utils），其余后续

> 请选择：E1 / E2 / E3

---

## 六、发版策略

- [ ] **F1.** 每阶段完成后分别发版（node-utils minor → plugins minor → utils major）
- [ ] **F2.** 全部迁移完成后一次性发版

> 默认：F2 ✅

---

## 七、CI

- [ ] **G1.** 新增 PR typecheck workflow（`pnpm typecheck`）
- [ ] **G2.** 暂不添加，仅本地 typecheck

> 默认：G1 ✅

---

## 八、其他

- [ ] **H1.** 迁移完成后删除 `docs/typescript-migration/` 方案文档
- [ ] **H2.** 保留方案文档作为历史记录

> 默认：H2 ✅

---

## 快速确认模板

复制以下模板，修改你的选择后回复：

```
确认执行 TypeScript 迁移：

范围：A1 A2 A3 A4（css 不迁移）
构建：B1（utils 改 tsup）
ES5：C1（移除，发 major）
strict：D1（先 false）
阶段：E1（全部执行）/ E2 / E3
发版：F2（完成后一次性发版）
CI：G1（加 typecheck workflow）
文档：H2（保留方案文档）
```

---

## 默认方案摘要

若你直接回复 **「按默认方案执行」**，我将：

1. Phase 1：添加根 tsconfig、typecheck 脚本
2. Phase 2：迁移 node-utils
3. Phase 3：迁移 plugins
4. Phase 4：迁移 utils（Webpack → tsup，移除 ES5，发 major 准备）
5. Phase 5：更新文档 + CI typecheck
6. 全部完成后提示你 `pnpm changeset` 发版

**不会自动发版**，代码改完后由你确认再 changeset。
