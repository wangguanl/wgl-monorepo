# 06 - 迁移后目录结构

## Monorepo 根目录

```
wgl-monorepo/
├── .github/workflows/
│   └── release.yml
├── .changeset/
├── docs/
│   └── typescript-migration/     ← 本方案文档（迁移完成后可归档）
├── packages/
│   ├── utils/
│   ├── plugins/
│   ├── node-utils/
│   └── css/
├── tsconfig.base.json            ← 新增
├── package.json
├── pnpm-workspace.yaml
├── turbo.json
├── RELEASE.md
├── PUBLISHING.md
└── MIGRATION.md
```

## packages/utils（迁移后）

```
packages/utils/
├── src/
│   ├── index.ts
│   ├── arrDiff.ts
│   ├── debounce.ts
│   ├── deepCopy.ts
│   ├── deepMerge.ts
│   ├── formatDate.ts
│   ├── hump.ts
│   ├── isArrayNested.ts
│   ├── isDateValid.ts
│   ├── isHtmlTag.ts
│   ├── parseUrl.ts
│   ├── recursionSegments.ts
│   ├── rem.ts
│   ├── unique.ts
│   ├── validation.ts
│   └── verifyVar.ts
├── dist/                         ← 构建产物（gitignore）
│   ├── index.js
│   ├── index.cjs
│   ├── index.d.ts
│   ├── index.d.cts
│   ├── es/
│   │   ├── debounce.js
│   │   ├── debounce.d.ts
│   │   └── ...
│   └── *.map
├── docs/                         ← VitePress（不变）
├── tsup.config.ts                ← 新增，替代 scripts/webpack.*
├── tsconfig.json                 ← 新增
├── package.json
└── README.md

❌ 删除：
├── scripts/                      ← Webpack 构建脚本
├── babel.config.js
└── test/index.html               ← 可选保留或迁移到 vitest
```

## packages/plugins（迁移后）

```
packages/plugins/
├── src/
│   ├── index.ts
│   ├── checkCapsLock.ts
│   ├── clickOutside.ts
│   └── ...（共 14 个 .ts）
├── dist/
│   ├── index.js / index.cjs
│   ├── index.d.ts / index.d.cts
│   ├── copyToClipboard.js
│   ├── copyToClipboard.d.ts
│   └── ...
├── docs/                         ← VitePress（不变）
├── tsup.config.ts                ← 从 .js 升级
├── tsconfig.json                 ← 新增
└── package.json
```

## packages/node-utils（迁移后）

```
packages/node-utils/
├── src/
│   └── index.ts
├── dist/
│   ├── index.js
│   ├── index.cjs
│   ├── index.d.ts
│   └── index.d.cts
├── tsup.config.ts
├── tsconfig.json
└── package.json

❌ 删除：
└── index.js                      ← 根目录单文件
```

## packages/css（不变）

```
packages/css/
├── src/
├── dist/
├── build/
├── gulpfile.js
└── package.json
```

## turbo.json tasks（迁移后）

```json
{
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"]
    }
  }
}
```

## 根 package.json scripts（迁移后）

```json
{
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "dev": "turbo run dev"
  }
}
```
