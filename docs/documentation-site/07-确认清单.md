# 07 - 确认清单

请逐项确认后回复，我再开始改代码。

## A. 总体方向

- [ ] **A1.** 同意合并为根目录单一 VitePress 站点
- [ ] **A2.** 同意删除 `packages/utils/docs`、`packages/plugins/docs`（内容迁至 `docs/packages/`）
- [ ] **A3.** 规划文档（`typescript-migration/`、`documentation-site/`）不进入对外站点

## B. 部署

- [ ] **B1.** 部署到 **monorepo GitHub Pages**（`https://wangguanl.github.io/wgl-monorepo/`）
- [ ] **B2.** 部署到 **新的独立仓库**（请填写仓库名：____________）
- [ ] **B3.** 旧 `page__package-utils` / `page__package-plugins` 保留 **HTML 重定向**
- [ ] **B4.** 旧独立仓库 **直接下线**，不做过渡

## C. 内容与范围

- [ ] **C1.** Phase 2 仅搬迁现有 utils / plugins 用例，不重写
- [ ] **C2.** Phase 3 为 node-utils、css 编写 **基础文档**（安装 + 用法）
- [ ] **C3.** 需要增加 `docs/guides/`（发版、迁移摘要等）
- [ ] **C4.** 批量修正文档中的旧包名 `wgl-utils` → `@wgl-m/utils`

## D. CI

- [ ] **D1.** PR 增加 `docs:build` 校验
- [ ] **D2.** push main 自动部署 GitHub Pages
- [ ] **D3.** 仅本地手动部署，暂不配 CI

## E. 脚本兼容

- [ ] **E1.** 根目录保留 `docs:utils` / `docs:plugins` 别名，均指向 `pnpm docs`（过渡期）
- [ ] **E2.** 立即移除别名，只保留 `pnpm docs`

---

## 回复模板

复制下面模板填写后回复即可：

```
确认执行统一文档站点方案。

A: A1✅ A2✅ A3✅
B: B1✅ B3✅
C: C1✅ C2✅ C4✅
D: D1✅ D2✅
E: E2✅
```

或说明需要调整的部分，例如：

> 部署用自定义域名 xxx.com，旧站不重定向
